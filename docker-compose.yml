version: '3'

services:
  users-service:
    build: ./users-service
    ports:
      - "8001:8000"

  orders-service:
    build: ./orders-service
    ports:
      - "8002:8000"
    depends_on:
      - users-service